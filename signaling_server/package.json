{
  "name": "remote-control-signaling-server",
  "version": "1.0.0",
  "description": "Signaling server for remote control app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "pm2:start": "pm2 start server.js --name remote-control-server",
    "pm2:stop": "pm2 stop remote-control-server",
    "pm2:restart": "pm2 restart remote-control-server"
  },
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
